详细说明:
描述：
这是一个供 Maya 使用的 Python 工具，允许用户在任意给定的 Nurbs 曲线上创建一个 Ribbon（丝带），并具有以下定制选项：
- 控制器数量
- 命名约定
- 关节数量
- 控制器的大小、形状和颜色
- 添加变形器
- 放在层级结构下
- 将 Ribbon 附着到两个端点
该 Ribbon 可用于游戏引擎。若要导出到游戏引擎，需要：
 1. 烘焙动画
 2. 删除每个绑定关节的约束
 3. 删除 "Extras" 组（可选）
 4. 删除控制器（可选）
 5. 导出到游戏